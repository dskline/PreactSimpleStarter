sudo: false

language: node_js

node_js:
  - 'stable'

cache:
  directories:
    - node_modules

script:
  - npm run build
  - npm test

after_success:
  - cat ./coverage/lcov.info | node node_modules/.bin/coveralls